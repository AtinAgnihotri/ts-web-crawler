{
  "name": "boot_dev",
  "version": "1.0.0",
  "description": "A small web crawler built in TS",
  "main": "src/index.js",
  "scripts": {
    "setup": "nvm use && npm i",
    "clean-setup": "rm -rf node_modules/ && nvm use && npm i",
    "test": "jest",
    "dev": "ts-node --esm src/index.ts",
    "build": "tsc",
    "clean-build": "rm -rf dist && tsc",
    "start": "tsc && node dist/index",
    "clean-start": "rm -rf dist && tsc && node dist/index"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "@types/jsdom": "^21.1.2",
    "@types/node": "^20.5.7",
    "jest": "^29.6.4",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "jsdom": "^22.1.0"
  }
}
